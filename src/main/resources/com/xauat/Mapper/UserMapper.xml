<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xauat.Mapper.UserMapper">

    <insert id="insertAndGetId" parameterType="com.xauat.Pojo.User" useGeneratedKeys="true" keyProperty="id">
        insert into user(username,name, gender, image, create_time, update_time)
            values(#{username},#{name},#{gender},#{image},#{createTime},#{updateTime})
    </insert>

    <delete id="deleteByIds">
        delete from user where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>
    <update id="update">
        update user
        <set>
            <if test="username != null and username!=''">username = #{username},</if>
            <if test="name!=null and name!=''"> name = #{name},</if>
            <if test="gender!=null"> gender = #{gender},</if>
            <if test="image!=null and image!=''">image=#{image},</if>
            <if test="updateTime!=null">update_time=#{updateTime}</if>
        </set>
        where id = #{id}
    </update>
</mapper>